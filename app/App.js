import 'react-native-gesture-handler';

// With expo-router the routing is file-based under the `app/` folder.
// The providers and error boundary are placed in `app/_layout.js` so this App.js
// no longer needs to handle NavigationContainer. Keep this file minimal so the
// bundler doesn't break in setups that still reference App.

export default function App() {
  return null;
}

